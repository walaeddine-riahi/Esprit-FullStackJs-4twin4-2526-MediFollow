apiVersion: v1
kind: ConfigMap
metadata:
  name: medifollow-config
  namespace: medifollow
  labels:
    app.kubernetes.io/name: medifollow
data:
  NODE_ENV: "production"
  NEXT_PUBLIC_APP_URL: "https://medifollow.example.com"
  APTOS_NETWORK: "mainnet"
  # Add additional non-sensitive configuration below
  PORT: "3000"
  LOG_LEVEL: "info"
